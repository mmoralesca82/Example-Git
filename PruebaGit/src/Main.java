import POO.Person;
import POO.*;


import java.util.ArrayList;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

       /* Scanner sc = new Scanner(System.in);

        ArrayList<User> user = new ArrayList<User>();

       user.add(new User((int) 100, "Manuel", (byte) 23, (long) 154895, (String) "mmoralesca"));
       user.add(new User((int) 101, "Patricia", (byte) 21, (long) 458525, (String) "pattyvs"));
       user.add(new User((int) 103, "Leito", (byte) 1, (long) 7896585, (String) "leitomv"));


       User userFound = new User((int) 0, "none", (byte) 0, (long) 0, "none");

        System.out.print("\nIngrese nickname: ");
        String nickname = sc.next();

        // Primera forma de búsqueda for

        boolean check = false;
        for(int i= 0; i < user.size(); i++){

           userFound = user.get(i);
           if(nickname.equals(userFound.getNickname())){
                System.out.print("El nombre del usuario es: "+userFound.getName()+"\n\n");
                check  = true;
                break;
            }
        }

        if(!check) {
            System.out.println("Nickname no encontrado.");
        }

        // Segunda forma de búsqueda for

        System.out.print("\nIngrese segundo nickname: ");
        nickname = sc.next();
        check = false;

        for(User usuario: user){

            if(nickname.equals(usuario.getNickname())){
                System.out.print("El nombre del usuario es: "+usuario.getName()+"\n\n");
                check  = true;
                break;git
            }
        }

        if(!check) {
            System.out.println("Nickname no encontrado.");
        }*/



    }
}